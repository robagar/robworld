#include "StdAfx.h"
#include "RobWorldApp.h"

int WINAPI WinMain(HINSTANCE hInstance, HINSTANCE hPrevInstance, LPSTR lpCmdLine, int nCmdShow) 
{
	RobWorldApp app;

	//try
	//{
		app.Run();
		return 0;
	//}
	//catch (Ogre::Exception& e)
	//{
	//	debug << "Fatal Ogre exception thrown: " << e.getFullDescription() << endl;
	//}
	//catch (std::exception& e)
	//{
	//	debug << "Fatal exception thrown: " << e.what() << endl;
	//}
	//return -1;
}