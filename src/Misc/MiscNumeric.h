#pragma once

// constants
extern const double pi;

/**
 * @brief Converts an angle measured in degrees to radians
 */
double deg2rad(double d);

/**
 * @brief Converts an angle measured in radians to degrees
 */
double rad2deg(double r);

/**
 * @brief Rounds a floating point number to n decimal places
 */
double round(double d, int n);


